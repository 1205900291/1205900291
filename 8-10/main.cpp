#include <iostream>

using namespace std;

void zero(const int bigIntegers[],int n)
{
    if(n==0)
      return 0;
      else
        return bigIntegers[];
}

int main()
{
    const int bigIntegers[]={};
    zero(bigIntegers,n);
    cout<<zero(bigIntegers,n);
    return 0;
}
